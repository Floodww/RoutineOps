// Единая преднастройка vitest.
//
// 🔴 Зачем: подписи в коде теперь идут через i18next, а язык выбирает LanguageDetector —
// то есть ОКРУЖЕНИЕ. В node-среде теста навигатора нет, детектор берёт язык откуда
// придётся (переменные окружения раннера), и тесты, проверяющие текст, начинают зависеть
// от машины: на одной «1–50 из 320», на другой «1–50 of 320». Так и вышло после перевода
// интерфейса — пять тестов покраснели, хотя код исправен.
//
// Язык прибиваем явно к ru: тесты сверяют русские формулировки, и делать это они должны
// детерминированно. Проверять сам перевод — работа гейтов в src/i18n (полнота словарей и
// отсутствие захардкоженного текста), а не косвенно через тесты пагинатора.
import i18n from "@/i18n/config";

await i18n.changeLanguage("ru");
