//go:build !enterprise

// Package screen (захват экрана и интерактивный сеанс) — enterprise-фича: удалённое
// подключение к рабочему столу отнесено к Enterprise решением владельца (роадмап, Этап 2).
// В open-core пакет пуст — все файлы под //go:build enterprise. Заглушка держит пакет
// непустым для `go build ./...`.
//
// Гейт: `make check-remote-tags` проверяет ОБА направления — во free-бинаре кодов сеанса
// нет, в enterprise они есть. Без такой проверки отсутствие фичи в срезе доказывалось бы
// только чтением build-тегов, а именно так уже один раз уехал бы enterprise-бинарь мимо
// всех гардов (см. комментарий про BINLEAK в scripts/export-free.sh).
package screen
