//go:build !enterprise

// Package screenframe (формат кадра интерактивного сеанса) — enterprise-фича:
// удалённый рабочий стол. В open-core редакции файлы пакета отсечены тегом сборки, и
// здесь остаётся только эта заглушка — иначе `go build ./...` спотыкается о пакет без
// единого файла.
//
// Гейт `make check-remote-tags` проверяет разъезд в обе стороны: во free-бинаре пакета
// нет ни в графе зависимостей, ни в строках, в enterprise — есть.
package screenframe
